## Errors

These are the exceptions raised by the SDK. `MeilisearchApiError` is raised when Meilisearch
returns an error response, and `MeilisearchCommunicationError` is raised when Meilisearch cannot
be reached at all. The token errors are raised when generating or validating tenant tokens.

## Errors API

::: meilisearch_python_sdk.errors
