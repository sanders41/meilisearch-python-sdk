## Settings Models

These are the models used when getting and updating index settings. `MeilisearchSettings` holds
all of the settings for an index and is what `get_settings` returns and `update_settings` accepts.
The remaining models are the values used within it, for example the embedder models are used to
configure vector search.

## Settings Models API

::: meilisearch_python_sdk.models.settings
